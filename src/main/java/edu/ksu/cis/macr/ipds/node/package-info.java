/**
 * Provides a single node launcher program that can start an agent-based simulation running on a single
 * device (i.e., a single node in the agent topology).
 *<p>
 * Each device node includes one or more autonomous agents running in a single JVM.
 *<p>
 * Each node is currently represented by a folder in the test case folder, e.g. H44 or N43.
 */
package edu.ksu.cis.macr.ipds.node;
