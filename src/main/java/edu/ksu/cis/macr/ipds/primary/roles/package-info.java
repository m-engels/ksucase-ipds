/**
 * Provides classes needed to define roles.
 *
 * Use communication capability interface names in role models.
 *
 * Use communication capability implementation (process) classes in agent.xml files.
 *
 * InternalCommunicationCapability should be included in all roles in the role models,
 * but does not need to appear in the agent.xml files.
 *
 */
package edu.ksu.cis.macr.ipds.primary.roles;
